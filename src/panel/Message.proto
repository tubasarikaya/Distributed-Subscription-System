syntax = "proto3";

package communication;

message Message {
    Demand demand = 1;
    Response response = 2;
}

enum Demand {
    CPCTY = 0;
    STRT = 1;
}

enum Response {
    YEP = 0;
    NOPE = 1;
}